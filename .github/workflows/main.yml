name: goapp
on: 
  push:
    branches: [ master ]
jobs:
  code-checkout:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: chode-checkout
        run: |
          echo "code run successfully"
