name: goapp
on: 
  push:
    branches: [ master ]
jobs:
  code-checkout:
    runs-on: ubuntu-latest
    steps:
      - name: code-checkout
        uses: actions/checkout@v2
        run: |
          echo "code checkout successfully"
